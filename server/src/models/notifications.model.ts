export interface Notification {
    id: string;
    userId: string;
    time: Date;
    eventId: string;
    message: string;
};

export interface createNotif {
    id: string;
    userId: string;
    time: Date;
    eventId: string;
    message: string;
};

